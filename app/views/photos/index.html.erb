<h1>Listing photos</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Descrioption</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @photos.each do |photo| %>
  <tr>
    <td><%= photo.name %></td>
    <td><%= photo.descrioption %></td>
    <td><%= photo.user_id %></td>
    <td><%= link_to 'Show', [photo.vacation, photo] %></td>
    <td><%= link_to 'Edit', edit_vacation_photo_path(photo.vacation, photo) %></td>
    <td><%= link_to 'Destroy', [photo.vacation, photo], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Photo', new_vacation_photo_path(params[:vacation_id]) %>
